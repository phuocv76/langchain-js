# ---------------------------------------------------------------------------
# Environment is configured per app — not in this repo root.
# ---------------------------------------------------------------------------
#
# Agent runtime (LangGraph + Hono API + CopilotKit runtime):
#   cp apps/agent/.env.example apps/agent/.env
#
# Web frontend (Next.js + CopilotKit chat):
#   cp apps/web/.env.example apps/web/.env
#
# See apps/agent/README.md and apps/web/README.md for variable details.
