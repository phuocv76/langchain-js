/**
 * All tools available to the kitchen agent.
 *
 * Empty in the skeleton. Add tools the same way the space product does:
 * define read-only schemas here, execute them in a middleware
 * `wrapToolCall` with the verified acting identity from the trusted agent
 * context — never from model-provided arguments.
 */
export const tools = [];
