/**
 * Shared Prettier configuration for the monorepo.
 * @type {import('prettier').Config}
 */
const config = {
  singleQuote: true,
  semi: true,
  trailingComma: 'all',
  printWidth: 80,
};

export default config;
