# Copy to web/.env.local
# URL of the `langgraphjs dev` server started with `npm run dev:agent` at the repo root.
LANGGRAPH_DEPLOYMENT_URL=http://localhost:2024

# Optional: only needed if you connect to LangSmith deployments.
# LANGSMITH_API_KEY=ls-...
